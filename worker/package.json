{
  "name": "cloudflare-workers-openapi",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "build": "cd .. && bun run build:local",
    "deploy": "bun run build && wrangler deploy",
    "dev": "wrangler dev",
    "cf-typegen": "wrangler types"
  },
  "dependencies": {
    "zod": "^3.24.1"
  },
  "devDependencies": {
    "@types/service-worker-mock": "^2.0.4",
    "wrangler": "^4.46.0"
  }
}
